apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: theengs

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - daemonset.yaml

commonLabels:
  app.kubernetes.io/part-of: home-automation
  app.kubernetes.io/managed-by: kustomize

# Optional: Add image tag override
# images:
#   - name: theengs/gateway
#     newTag: "1.0.0"
